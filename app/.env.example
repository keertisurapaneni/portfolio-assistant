# Supabase Configuration (these are public/safe — Row Level Security protects data)
# All API keys (Finnhub, Gemini) are stored as Supabase secrets on the server
VITE_SUPABASE_URL=REPLACE_WITH_YOUR_SUPABASE_PROJECT_URL
VITE_SUPABASE_ANON_KEY=REPLACE_WITH_YOUR_SUPABASE_ANON_KEY
