# Discrepancy Fixes Log

**Purpose:** Track every fix made to resolve Today's Activity vs IB discrepancies.  
Read this file FIRST before making any new fix. If you are about to fix something that already appears here, STOP — you are in a regression loop.

---

## How to Use This File

Before making any fix:
1. Search this file for the ticker/symptom
2. If it already appears — find the root cause column and read it before touching anything
3. After fixing — add a new row immediately

---

## Fix Log

| Date | Symptom | Root Cause | Files Changed | PR | Side Effects / Regressions Introduced |
|------|---------|-----------|---------------|----|---------------------------------------|
| Jul 2 | FCX accidental short (-121 shares @ $59.51 at 2:14 PM) — untracked position in IB, -$70.61 loss | Jun 9 FCX SWING_TRADE was closed as `duplicate_close` but the GTC SL bracket order was never cancelled. Today FCX dropped to $59.51, orphaned stop fired, sold 121 shares from flat account → accidental short. Same root cause as NFLX Jun 17 (swing_expiry) but via `duplicate_close` code path — which also never cancels brackets. | Manually covered in IB: BUY 121 FCX MKT @ $60.15. Loss: -$70.61. **No paper_trade record** (fully manual, no IB order ID in our system). The -$70.61 will appear only in IB's realized P&L, not in Today's Activity. **Code fix (reconcile-executions.ts + scheduler.ts)**: added `cancelOrphanedBracketOrders()` — on every startup (60s after IB connects) it queries all CLOSED/STOPPED/TARGET_HIT trades from last 30 days and sends `cancelOrder()` for any bracket order IDs still set. Safe to call on already-filled/cancelled orders (IB ignores silently). | `auto-trader/src/lib/reconcile-executions.ts` (new `cancelOrphanedBracketOrders()`), `auto-trader/src/scheduler.ts` (60s startup call) | pending | -$70.61 untracked loss. cancelOrphanedBracketOrders only runs on startup — if auto-trader runs for days without restart, new DB-patched records won't be caught until next restart. Accept this tradeoff (startup coverage is sufficient for most cases). |
| Jul 2 | ORCL (49+48 shares, -$1,235) and SPY day trade (13 shares, -$65) stopped out in IB at 11:22 AM but never appeared in Today's Activity — IB showed -$1,302.82, our UI showed +$1,248.70 | `execDetails` callback dropped for GTC bracket SL fills mid-day (same root cause as Jun 29 QQQ). Hourly `reconcileMissedBracketFills()` calls IB directly and found the SL fills (78497/78494 for ORCL, 91492 for SPY) — but line 722 skipped them because the **loss-cut loop had pre-stamped `ib_close_order_id` with rejected code-200 order IDs** (94275→95360 etc). Reconciler saw `ib_close_order_id` set and assumed "already handled". | `auto-trader/src/lib/reconcile-executions.ts` line 722: changed `if (trade.ib_close_order_id) continue` → `if (trade.ib_close_order_id === exec.orderId.toString()) continue`. DB patches: ORCL 49-share → STOPPED -$622.31, ORCL 48-share → STOPPED -$609.12, SPY day trade → STOPPED -$65.39. | pending | none |
| Jul 1 | Re-enable AMD/PLTR in VWAP scalp scanner (Mon–Thu) | Previously removed after Jun 29 losses from APP/MSFT/GOOGL/PLTR/HOOD options expiring worthless. Root cause was: (1) PR #463 used `getNearestFridayExpiry()` for ALL tickers, (2) pre-#475 management cycle skipped near-worthless options on expiry day (Greeks=null → no close). Both fixed. Now AMD/PLTR use `getNearestFridayExpiry()` Mon–Thu and are closed SAME DAY by 3:45 PM EOD sweep — no overnight hold. | `auto-trader/src/lib/options-scalp.ts` | pending | Risk: if 3:45 PM EOD close order times out AND IB is disconnected, position stays open overnight. Mitigation: keep list to 2 tickers, monitor first week. Do NOT expand list (APP, MSFT, etc.) without confirming EOD close reliability over 5+ trading days. |
| Jun 9 | ADBE not showing in Today's Activity | `closed_at=null` after partial close — `TodayActivityTab` filters `closed_at >= today` | `scheduler.ts` partial close logic | #431 | Incorrect: set close fields on original record. Caused wrong P&L (full qty × close_price). Required PR #434 to fix. |
| Jun 9 | RKLB showing +$298.68 green P&L (phantom) | `syncPositions` matched RKLB OPT position in IB to RKLB STK paper_trade — no `secType` check | `scheduler.ts` `getEnrichedPositions()`, `positions.find()` | #432 | **REGRESSION:** Credit spreads are OPT in IB. STK-only filter caused credit spreads to appear "missing" → 7 spreads falsely closed next morning (SOXL, ALAB, AMD, etc.) |
| Jun 10 AM | 7 credit spreads falsely closed pre-market (SOXL, ALAB, AMD, etc.) | Side effect of PR #432 STK filter — credit spreads are OPT, were no longer found by `syncPositions`, triggered "position missing" fallback | `scheduler.ts` `_syncPositionsForAccount` | #433 | DB repair required: manually reopened 7 records |
| Jun 10 | ADBE and NEM P&L/qty wrong after partial loss cuts | `scheduler.ts` partial close was mutating original record (`remainingQty` × `close_price` = wrong P&L). `reconcileGhostCloses` then closed NEM prematurely using ghost from partial fill | `scheduler.ts` partial close block (~line 7282) | #434 | DB repair required: created child records for ADBE (orders 37825, 39548), NEM (order 43825), reopened RKLB ff945d9a |
| Jun 10 | ASTS loss missing from Today's Activity | `TodayActivityTab.tsx` dedup logic (lines 190-196) filtered `ib_fill_auto_created` ghosts when a real trade existed for same ticker | `paper_trades` DB: changed ASTS ghost `close_reason` from `ib_fill_auto_created` to `loss_cut` | none (DB patch) | None |
| Jun 10 | Duplicate trades from "Execute All" multi-click (FCX, PLTR, QQQ, RKLB, SPY) | No idempotency on "Execute All" button | `TodayActivityTab.tsx` | #429 | None |
| Jun 10 | External trade (QQQ) marking auto-trader signals as "traded" | `TradeIdeas.tsx` didn't filter `strategy_video_id` / `strategy_source` trades from `tradedTickers` | `TradeIdeas.tsx` | #428 | None |
| Jun 10 | Credit spread ghost records created when close fills arrive | `credit-spread-scanner.ts` called `recordTradeClose` BEFORE `ib_close_order_id` was stamped — fills arrived in the ~100ms window, trigger found no match, created ghost | `credit-spread-scanner.ts` | #435 | None |
| Jun 10 | `reconcileGhostCloses` closed NEM prematurely (IB still held 3 shares) | `reconcileGhostCloses` closed real trade when ghost existed, without checking IB live positions | `reconcile-executions.ts` | #435 | None |
| Jun 10 | NBIS `6ab19f67` spuriously closed with `close_reason=manual`, `pnl=0` | **Root cause still unresolved.** Attribution: scheduler SUBMITTED detection path suspected, but CREDIT_SPREAD is `continue`d before that block. Reopened manually. | DB patch: reopened `6ab19f67` | none | Needs investigation — if root cause not found, will recur |
| Jun 10 | Credit spread P&L wrong in Today's Activity (estimated Greeks, not actual fills) | `manageCreditSpreadPositions` called `recordTradeClose` immediately after placing close order with estimated P&L. Trigger couldn't correct it — combo fills have `realized_pnl=null`, trigger section 4 requires `realized_pnl IS NOT NULL` | `credit-spread-scanner.ts`, new trigger section (4b) | #436 | None known |
| Jun 11 | ADBE/NEM loss cut child records showing "BUY" signal instead of "SELL" | PR #434 child record insert used `signal: trade.signal` — inherited `'BUY'` from original LONG_TERM position. Loss cut is a sell action, should be `'SELL'` | `scheduler.ts` line 7301 | #437 | None |
| Jun 11 | OPTIONS_SCALP ORB scanner placed 0 trades silently for 2+ days | `range=2d` returns only ~100 bars; `ORB_SMA200_PERIOD=200` requires ≥200, so `get5mSmaDirection()` always returned null — every ticker skipped as "chopping around 200 SMA". **Validated live**: `range=5d` returns 334 bars, `range=2d` returns 100. | `yahoo-finance.ts`: added `'5d'` to range type. `options-scalp.ts`: `'2d'`→`'5d'`, restored `ORB_SMA200_PERIOD=200`, added data-unavailable log | #438+#439 | None — 200 SMA now actually computable |
| Jun 11 | META short day trade: `ib_close_order_id` stamped with TP order ID instead of SL order ID | Position-sync used `tpId \|\| slId` (TP first, truthy) → always passed TP order ID to `recordTradeClose` even when SL fired. `recordTradeClose` looked for ib_fills by TP order ID, found nothing, fell back to formula. ib_close_order_id got set to wrong order → trigger can never auto-correct. P&L difference was ~$2 (commissions) — **commission differences are acceptable, not treated as discrepancies**. The ib_close_order_id correctness matters for trigger linkage. | `scheduler.ts`: `bracketCloseOrderId` now picks the order ID of whichever fill confirmed. DB patch: `b6ae0e75` — set `ib_close_order_id='44191'`, `pnl=-107.09` | #443 | None. Commission-level P&L differences (~$2) are acceptable. |
| Jun 11 | ADBE loss cut showing +$120.92 instead of -$120.92 (sign-flipped P&L) | Partial loss cut child records have `signal='SELL'` (PR #437 display fix). `trade-closer.ts` line 92 used `signal === 'BUY'` to determine `isLong` → child's 'SELL' → formula used `(fillPrice - closePrice) * qty` = positive instead of `(closePrice - fillPrice) * qty` = negative. `ib_fills.realized_pnl` was null (commission report pending) so trigger never corrected it. Fix: added `positionDirection?: 'LONG'\|'SHORT'` to `CloseTradeParams`; partial loss cut call site passes `ibPos.position > 0 ? 'LONG' : 'SHORT'`. **Root architecture debt:** `signal` serves dual purpose (entry direction + close-action display). Tracked as TODO for `display_action` column. | `auto-trader/src/lib/trade-closer.ts`, `auto-trader/src/scheduler.ts`. DB patch: `f4436094` pnl → -120.93 | #442 | None. `positionDirection` is optional — all existing callers unaffected. **If this P&L sign-flip recurs on any loss cut record, the commission report in ib_fills will eventually overwrite it with the correct value when IB sends it.** |
| Jun 11 | OPTIONS_SCALP 0DTE never implemented — always buying multi-day weekly options | `getNearestWeeklyExpiry()` explicitly picked "next Friday ≥1 day out". Kay Capitals uses same-day expiration (0DTE) exclusively. SPY has 0DTE chains Mon–Fri. Multi-day options have expensive premium and were held overnight when EOD close failed pre-Jun-5. Renamed to `getTodayExpiry()` returning today's ET date. Zero 0DTE trades have ever been placed. | `auto-trader/src/lib/options-scalp.ts` | #444 | First time 0DTE orders will be placed. If IB rejects same-day chain, `executeScalp` returns false → graceful skip (no crash). |
| Jun 12 | GS SWING_TRADE (qty=10, +$347.83) missing from Today's Activity | GTC TP order 39711 fired at 05:48:34 ET before auto-trader connected. Auto-trader missed execDetails callback — no entry in `ib_fills` for the close. `closed_at=null` → `getTodayTrades()` filter missed it. Root cause: `runEndOfDayReconciliation` only queries trades with `opened_at/filled_at >= today` — misses multi-day swing positions like GS (filled Jun 10). | DB patch: `3250c4e7` — `status=TARGET_HIT`, `ib_close_order_id=39711`, `closed_at=2026-06-12T09:48:34Z`, `close_price=1061.04`, `pnl=347.83`, `close_reason=target_hit`. **Permanent fix (Jun 12):** `reconcileMissedBracketFills()` in `reconcile-executions.ts` — queries ALL FILLED trades with bracket order IDs (no date filter), runs at startup +45s via `scheduler.ts`. | `auto-trader/src/lib/reconcile-executions.ts`, `auto-trader/src/scheduler.ts` | (pending PR) | GS qty=11 tranche (`e4fafd99`) still open — bracket untouched. IB auto-cancelled SL 39712 when TP fired. |
| Jun 12 | GOOGL OPTIONS_PUT (qty=1, +$183.77) missing from Today's Activity | `ibBuyToCloseOption` timed out (fill arrived 195+ log lines later). Old code returned `null` on timeout, discarding `orderId`. Caller skipped `recordTradeClose` and never stamped `ib_close_order_id`. Postgres trigger fired 3× (orderStatus insert, execDetails insert, commissionReport update) but found no `ib_close_order_id` match on `paper_trade` each time. | DB patch: `efe775bf` — `status=TARGET_HIT`, `ib_close_order_id=50092`, `closed_at=2026-06-12T15:39:59Z`, `close_price=4.00`, `pnl=183.77`, `close_reason=target_hit`. **Permanent fix (Jun 12):** `IBCloseResult` discriminated union + `stampPendingBtcOrder()` helper in `options-manager.ts`. All 9 call sites updated with `if (ibClose.timedOut)` guard — stamps `ib_close_order_id` immediately, defers `recordTradeClose` to the Postgres trigger. | `auto-trader/src/lib/options-manager.ts` | (pending PR) | Commission report for 50092 may still arrive; trigger may update pnl but delta will be within $1-2 tolerance. |
| Jun 11 | OPTIONS_PUT wheel scanner placed 0 trades for 9 days (May 29 – Jun 11) | Watchlist intentionally cleaned up to 47 active tickers, but all are Tech/Comm Services/Consumer Discretionary. Tech correction → 16 tickers `below_sma50`. 9 open positions (AAPL, AMAT, ADI, CRDO, ORCL, RBRK + 3 more) fill Technology `sector_limit` (MAX=3), blocking 10 more. All 47 blocked — self-locking state. No defensive sector fallback left. | DB only: activated KO (Consumer Staples +6.2% above SMA50), JNJ and ABBV (Health Care +3.9%/+7.3%). Sectors with 0 open positions, tickers you'd want to own if assigned. Migration: `20260611000001_watchlist_activate_defensive_tickers.sql` | none | **Do NOT re-add bulk defensive tickers** — watchlist cleanup was intentional. If scanner goes dry again in future: check which non-Tech sectors have capacity first, then add 1-3 names only. |
| Jun 15 | OPTIONS_SCALP placed 0 trades all day — every ticker logged `quote=null` | Yahoo Finance `v7/finance/quote` endpoint now returns 401 Unauthorized for all unauthenticated requests. `fetchQuote()` silently returned `null`, causing scalp scanner to skip every ticker at the data-availability check. The `v8/finance/chart` (intraday bars) endpoint is unaffected. | `auto-trader/src/lib/yahoo-finance.ts`: `fetchQuote` now tries v7 first; on failure falls back to last 5-min bar close for `price` and first bar of day for `open`. `beta`/`high52w`/`earningsTs` return null in fallback — all callers already guard these with `!= null`. | #447 | Callers that use `beta` for screening (options-scanner) now skip the beta filter when v7 is down — conservative (might let a high-beta ticker through, but won't block trades). |
| Jun 15 | Today's Activity: closed trades showing BUY badge instead of SELL | Signal badge always displayed `trade.signal` (entry signal). Closed long positions were bought (BUY signal) but the exit action is SELL. User confirmed FCX, NFLX, ADBE should show SELL. | `app/src/components/PaperTrading/tabs/TodayActivityTab.tsx`: for closed trades, flip the displayed signal — BUY→SELL (sold to exit long), SELL→BUY (covered short). Active trades unchanged. | #446 | Display-only change. Underlying `trade.signal` in DB is unchanged (still BUY). No impact on filtering, execution, or P&L logic. |
| Jun 15 | Today's Activity: closed trades showing open time (09:36 AM) instead of close time | Time column rendered `trade.filled_at ?? trade.opened_at` unconditionally. Day trades filled at 9:36 AM but closed at 3:55 PM EOD sweep showed 09:36 AM. | `app/src/components/PaperTrading/tabs/TodayActivityTab.tsx`: closed trades now show `closed_at`; active trades keep `filled_at`. Sort-by-time also updated to match. | #449 | None. |
| Jun 15 | Day trade ETF TP never hit intraday — trades always drift to EOD close | Max stop cap for DAY_TRADE was 1.5% for all tickers → TP set to 3% (2× stop). SPY/QQQ/IWM have ~0.5–1.5% daily range so 3% TP is almost never reached. Trades floated all session and closed at EOD via sweep. Only UCTT (TP=1.6%) ever hit its bracket TP in weeks of trading. | `auto-trader/src/scheduler.ts`: ETFs (SPY, QQQ, IWM, DIA, SMH, GDX, GLD, TLT, SOXL, TQQQ, NVDL, TSLL, sector ETFs) now capped at 0.75% stop → 1.5% TP. Individual stocks keep 1.5% cap → 3% TP. | #448 | With tighter stop (0.75%), ETF trades will stop out sooner when move goes against us — smaller losses but also fewer "drift and recover" situations. Monitor first week to confirm TP actually gets hit. |
| Jun 17 | ALL options scalp orders code-200 (no security definition) on non-Friday | `getTodayExpiry()` returned today's calendar date (e.g. Wednesday Jun 17). Individual stocks (AMD, COIN, TQQQ, MSFT, SPY, QQQ, etc.) only list Friday weekly expirations. IB returns code-200 "No security definition" for any non-Friday expiry on those tickers. All 6 VWAP scalp orders cancelled. | `auto-trader/src/lib/options-scalp.ts`: `getTodayExpiry()` replaced by `getNearestFridayExpiry()` — always uses nearest Friday expiry for ALL tickers consistently. On Friday this is 0DTE; Mon–Thu it is the upcoming weekly. Removes per-ticker daily-options logic. | #463 | On Fridays the behavior is unchanged (0DTE). Mon–Thu trades now use next-Friday options (1–4 DTE) instead of 0DTE — these have slightly more time value but are resolvable for all tickers. |
| Jun 17 | IB drops mid-scan: NFLX ghost short (183×@$78.91) trips code-200 circuit breaker | Reconciler BUYs 183 NFLX to cover orphaned short → code-200 ("no security definition") every cycle. 2 NFLX failures + 1 other = 3 consecutive code-200 orders → circuit breaker forced disconnect. VWAP scan running at same time lost 4 tickers (APP, ALAB, MSTR, SOXL). Root cause of ghost: see Jun 17 row below. | `auto-trader/src/ib-connection.ts`: `CODE_200_RECONNECT_THRESHOLD` 3→8 so 2 NFLX failures don't trip it. `auto-trader/src/lib/options-chain.ts`: removed `if (!isConnected()) return null` early-exit in `findAtmStrike` — pure-BS Attempt 2 now runs even during brief disconnect. **Manual action required**: clear NFLX 183-share short in IB paper account. | #462 | NFLX ghost will keep retrying every reconcile cycle until manually cleared. Threshold raise buys time but doesn't fix root cause. |
| Jun 17 | NFLX orphaned short (183 @ $78.91) root cause: swing_expiry didn't cancel bracket orders before placing new close order | NFLX SWING_TRADE (35688) opened Jun 5 with bracket TP=35689 / SL=35690. Closed Jun 15 via `swing_expiry` with new order 52363. `expireStaleSwingTrades()` called `stampAndPlaceClose` but never cancelled TP/SL first. SL bracket (35690) remained live in IB. NFLX dropped to $78.92 on Jun 16 → stop fired against a flat position → created -183 short. Same pattern as CSCO/AMAT double-sell (May 15). EOD, soft-close, and trailing-stop already cancel brackets; swing_expiry was the only outlier. | `auto-trader/src/scheduler.ts` `expireStaleSwingTrades()`: added `swingConn.cancelOrder(ib_tp_order_id)` + `swingConn.cancelOrder(ib_sl_order_id)` before `stampAndPlaceClose`, matching the pattern already used in EOD/soft-close/trailing-stop. | (pending PR) | All future swing_expiry closes now cancel brackets first. NFLX short still needs manual close in IB. |
| Jun 17 | VWAP Scalp + ORB Scalp: `no ATM strike found` for every ticker on 0DTE day | `daysToExpiry("20260617")` returns 0 from midnight onward. `atmBsPrice` had `if (dte <= 0) return null` — killed both the IB-chain BS fallback and the interval-strike BS path inside `findAtmStrike`. Every VWAP signal (SMH, AAPL, NVDA, AVGO 10:00 AM) was correctly detected but blocked here. ORB scalp would have hit same wall if anything cleared NTZ. | `auto-trader/src/lib/options-chain.ts`: `atmBsPrice` — `dte <= 0` → `dte < 0`; when `dte === 0`, compute remaining hours to 4 PM ET close and use `T = (hoursRemaining/24)/365` so BS has a positive time value. Min 0.25h floor prevents divide-by-zero near close. | #461 | Applies to any 0DTE day. BS ATM pricing for 0DTE is lower than normal (less time value) so premiums will be small — that's expected and correct. |
| Jun 16 | OPTIONS_SCALP ORB retest never triggered for 7+ trading days (Jun 9–16) | `ORB_RETEST_BUFFER = 0.15` was a fixed dollar amount. For SPY at $750 that's 0.02% — impossibly tight for a 5-min bar retest. Also no logging when a retest WAS found but price moved away or volume was high — silent near-misses made the issue invisible. Root cause chain: Yahoo v7 401 (Jun 5–14, fixed PR #447) was the primary blocker; this buffer issue was secondary but would have blocked many setups even with working quotes. | `auto-trader/src/lib/options-scalp.ts`: `ORB_RETEST_BUFFER=0.15` → `ORB_RETEST_BUFFER_PCT=0.0015` (0.15% of ORB level). SPY $752→$1.13, TSLA $280→$0.42, AMD $130→$0.20. Added diagnostic logs when retest found but price moved away or volume too high. `detectOrbSetup()` now accepts optional ticker param. Removed unused `INTRADAY_MOVE_MIN_PCT`. | (pending PR) | First test: tomorrow 9:45 AM ET. Both this fix AND PR #447 (Yahoo quote fallback) must be in place for scalps to fire. |
| Jun 16 | OPTIONS_PUT mild-loser puts had no exit gate between 21 DTE and expiry | 21 DTE block intentionally held mild losers (loss < premium) to ride theta. No forced close between 21 DTE and 0 DTE. Positions could expire ITM over a weekend, triggering assignment with no close order in IB. | `auto-trader/src/lib/options-manager.ts`: DTE ≤ 3 forced buy-to-close (`3dte_forced_close`) added inside the mild-loser branch. | (pending PR) | Mild losers held from 21 DTE to 4 DTE still ride theta as intended; only the final 3 days are force-closed. Very small buyback cost expected at 3 DTE. |
| Jun 16 | OPTIONS_PUT ITM expiry recorded as `expired_worthless` (wrong P&L) | `dte ≤ 0` block used fixed `expiredPnl = premiumCollected * 100` for all short puts regardless of moneyness. ITM puts should be `assigned`. | `auto-trader/src/lib/options-manager.ts`: at expiry, fetch live quote; stock < strike → `handleAssignment()` (was dead code, now called); stock ≥ strike → `expired_worthless`. | (pending PR) | `handleAssignment()` activated. DTE ≤ 3 forced close + 21 DTE management should prevent most ITM expirations; this is a last-resort P&L correctness fix. |
| Jun 16 | OPTIONS_CALL (covered call) ITM expiry recorded as `expired_worthless` | Covered call `dte ≤ 0` always kept full premium with `expired_worthless`. If stock closes above call strike, shares are called away — P&L = premium + (call_strike − acquisition_price) × 100. | `auto-trader/src/lib/options-manager.ts` (callData loop): fetch live quote; stock > call_strike → `called_away` with correct share P&L; else → `expired_worthless`. | (pending PR) | Wheel cycle P&L now correctly accounts for both the premium leg and the share delivery on call-away. |
| Jun 16 | Credit spreads: no backstop if 21 DTE close failed until DTE = 0 | If IB order placement failed every cycle, spread reached DTE ≤ 0 open indefinitely with no DB close. | `auto-trader/src/lib/credit-spread-scanner.ts`: `dte ≤ 0` block added before main exit rules. Settles as `expired_worthless` / `expired_partial_loss` / `expired_max_loss` based on stock moneyness vs spread legs. Falls back to max-loss if quote unavailable. | (pending PR) | DTE computed before `getStockQuote` call so backstop fires even if quote fails. Normal 21 DTE path unchanged. |
| Jun 16 | "Execute All" placed both BUY and SELL for PLTR, QQQ, SPY, NVDA in same session — IB netted opposing positions (FIFO), paper_trades tracked each side independently with mismatched cost basis. App P&L = +$255.55, IB P&L = +$395.18, gap = +$139.63. | **Root cause: entry trigger check** (lines ~5079–5109) was INSIDE `if (!skipConfirmationGates)`. "Execute All" sets `skipConfirmationGates = true`, bypassing the price-level check entirely — both sides of Somesh's two-sided signals (buy above X, sell below Y) executed at market regardless of whether price had reached the signal level. Secondary issue: opposing-position guard was also inside the same skipConfirmationGates block, but restoring it unconditionally would break valid dual-signal strategies. Fix: removed `!skipConfirmationGates` from the entry trigger check only — price levels are now ALWAYS enforced. Opposing-position guard restored to its original conditional position (secondary check). | `auto-trader/src/scheduler.ts` lines ~5079 and ~5088: removed `!skipConfirmationGates` from entry trigger conditions. | (pending PR) | **DB patched 2026-06-16**: (1) Created synthetic `ib_fifo_netting` paper_trade records for PLTR +$103.90 and NVDA +$104.89 netting closes. (2) Updated PLTR SHORT pnl $296.06→$54.18 (from ib_fills order 53112 realized). (3) Updated PLTR BUY pnl -$134.52→-$1.20 (IB FIFO: 1 LONG share closed, not 114). (4) Updated QQQ SHORT pnl $145.00→$18.60 (IB screenshot). (5) Updated QQQ scanner LONG pnl -$67.92→+$18.69 (ib_fills order 53975: scanner LONG was closed via netting, not SL). PLTR and NVDA now match IB exactly. QQQ has $3.58 residual (commission report for order 53116 pending — will self-correct). NVDA SHORT and SPY SHORT still open — P&L auto-corrects via trigger when they close. |
| Jun 16 | `persistEvent` PERMANENT failure for QQQ and WDC: "Could not find the 'current_price' column of 'auto_trade_events' in the schema cache" | `current_price`, `orb_high`, `orb_low`, `vwap` fields were being spread at the top level of the `persistEvent` `extra` object (lines 3939, 3947 in scheduler.ts). `createAutoTradeEvent` passes the full object to Supabase insert, which tries to find a column for every key. These fields don't exist as columns — they must go in `metadata`. | `auto-trader/src/scheduler.ts` lines 3939, 3947: moved `current_price`/`orb_high`/`orb_low`/`vwap` into `metadata: {}` | (pending PR) | Events for QQQ and WDC ORB boundary plays from Jun 16 are permanently lost (no retry). Paper_trades and trade execution were unaffected — only event log entries missing. |
| Jun 16 | NFLX orphaned short (183 @ $78.91) — auto-trader tried to BUY cover on every startup but IB rejected with "No security definition has been found" (code=200). Multiple restarts today → multiple failed attempts, no DB event logged. | NFLX SHORT at $78.91 is a paper-account artifact (very old position, IB can no longer process it as a tradeable contract). The `reconcileIBShorts` error handler only logged to console; no deduplication, no DB event. | `auto-trader/src/scheduler.ts` `reconcileIBShorts` error handler: detect `code=200`/`No security definition` → create deduplicated critical `auto_trade_events` entry (max 1 per day) with manual-IB-close instruction. | (pending PR) | NFLX position remains in IB — **must be closed manually from IB desktop/mobile**. The error event will appear in Today's Activity once per day until manually resolved. |
| Jun 16 | NFLX CRITICAL event still firing on every restart despite dedup fix | Dedup `ilike` pattern was `%no security definition%orphaned short%` — requires "security definition" to appear BEFORE "orphaned short" in the message. Actual message has them in the opposite order ("NFLX orphaned short … — no security definition"). Pattern never matched, so `existingAlert` was always empty, event fired every restart. | `auto-trader/src/scheduler.ts` line ~1584: changed pattern to `%no security definition%` (single anchor, no ordering requirement). | #456 | None. |
| Jun 16 | DNTH orphan long (14 shares) — `reconcileIBLongs` created duplicate warning events on every restart (no deduplication). | Warning event created unconditionally each startup in `reconcileIBLongs` when `unknownPositions.length > 0`. No date-based dedup like the after-hours short alert. | `auto-trader/src/scheduler.ts` `reconcileIBLongs`: deduplicate warning event — only log once per calendar day. | (pending PR) | DNTH long remains in IB (correct — longs are NOT auto-closed). **Must be resolved manually from IB.** No P&L impact since DNTH is an unrealized long with no paper_trade. |

| Jun 17 | Bear Spread positions (RKLB 135/95, RKLB 110/80, NOW 125/95, NBIS 230/170) manually closed in IB — DB not updated | Positions had `BEAR_PUT_DEBIT_HOLD` guard blocking auto-close. User manually closed all 4 in IB mobile. DB still showed `status=FILLED`. IB fills (from screenshot): NOW 125/95 → 6 contracts @ net $20.60 (SELL $24.30 / BUY $3.70); RKLB 135/95 → 3 contracts @ net $25.00 (09:53) + 2 contracts @ net $23.85 (12:00), wtd avg $24.54; RKLB 110/80 → 7 contracts @ net $10.51 (SELL $12.91 / BUY $2.40); NBIS 230/170 → 3 contracts @ net $12.00 (SELL $18.35 / BUY $6.35) — **previously patched incorrectly at $0.50**, corrected here. | DB patch (Node.js): NOW 125/95 → CLOSED, close_price=20.60, pnl=+$5,820; RKLB 135/95 → CLOSED, close_price=24.54, pnl=+$3,150; RKLB 110/80 → CLOSED, close_price=10.51, pnl=-$4,683; NBIS 230/170 → CLOSED, close_price=12.00, pnl=+$660. Total Bear Spread P&L: +$4,947. | none | NBIS 240/180 (Bull Put, Jul 17, 3 contracts) remains open — this is a legit position, NOT a Bear Put. The 4 closed positions had `BEAR_PUT_DEBIT_HOLD` in `notes` which auto-close correctly skipped. |
| Jun 16 | Credit spreads RKLB 135/95, RKLB 110/80, NOW 125/95, NBIS 230/170 showing as "Bear Spread" in IB (LONG higher put + SHORT lower put = Bear Put Debit) instead of Bull Put Credit | Root cause not definitively determined from code alone. Two candidates: (1) `Promise.all` concurrent `resolveOptionConId` calls had a race condition where conIds were returned for the wrong leg (sellConId ↔ buyConId swapped) due to concurrent IB API event listener interference. (2) IB `NonGuaranteed` combo routing filled legs independently and paper trading simulation produced leg reversals non-deterministically. Same session (orders 32569, 32572) placed NOW as Bear Put and CRDO as Bull Put with identical code — rules out static ordering bug. | `auto-trader/src/ib-connection.ts`: (1) Changed `Promise.all([resolveOptionConId, resolveOptionConId])` to SEQUENTIAL awaits to eliminate concurrent listener cross-wiring. (2) Added expiry mismatch validation — aborts if sell/buy legs resolve to different expiry dates (would create a diagonal spread). (3) Added resolved-conId + expiry logging for every order placed. `auto-trader/src/lib/credit-spread-scanner.ts`: (1) Added close guard that skips positions with `notes=BEAR_PUT_DEBIT_HOLD`. (2) Added diagnostic logging showing `isConnected()` state + all fields at every close attempt (was completely silent before — 0 "buy-to-close" log entries in 9166-line log despite 8 triggers firing). DB: 4 Bear Put positions marked `notes=BEAR_PUT_DEBIT_HOLD`. | (pending PR) | **The 4 Bear Put positions remain open in IB and must be closed manually.** Auto-close is intentionally blocked — a BUY close order with current leg convention would create a NEW Bear Put spread on top, not close the existing one. Diagnostic logging will reveal the exact failure path on the next market open cycle. Close the 4 positions in IB manually when convenient. |
 — spreads could be entered in crushed-IV (IVR=10) environments where premium is inadequate | No IVR gate in `scanTickerForSpread()`. Best practice: IVR ≥ 30 min before selling any vertical spread. | `auto-trader/src/lib/credit-spread-scanner.ts`: added `MIN_IV_RANK = 30` constant + `getStoredIvRank()` helper + Gate 4.5 between earnings check and spread strike fetch. Null IVR (< 10 data points) passes through to build history. | (pending PR) | Tickers with thin IV history (< 10 days of `options_iv_history`) are still allowed through — conservative choice to not block new watchlist additions. |
| Jun 16 | Credit spread position manager ran every 30 min for weeks but never closed any position — stop loss and profit take never fired | Three stacked bugs: (1) `getStockQuote` used Finnhub only — when rate-limited (55/min shared limit), all 11 positions hit `if (!quote) continue` silently. (2) When greeks failed, fallback to `currentSpreadValue = netCredit` made P&L = 0 every cycle — no rules fired even for deeply ITM spreads. (3) Stop loss threshold `pnlTotal ≤ -maxGainTotal` is equivalent to `spreadValue ≥ 2× credit` — for high-credit spreads (credit > 50% of width), this is HIGHER than the spread's max possible value, making stop unreachable. | `auto-trader/src/lib/credit-spread-scanner.ts`: (1) `getStockQuote` now falls back to Yahoo Finance (`fetchQuote`) when Finnhub fails; (2) intrinsic-value fallback added when greeks fail — estimates spread value from stock price vs strikes + 10% of width as time-value buffer; (3) stop loss now uses `min(2× credit, 90% of width)` as the stop threshold — capped so it's always reachable for high-credit spreads; (4) per-position diagnostic log added each cycle. | (pending PR) | Intrinsic fallback underestimates value for ATM positions with significant time value (e.g. short strike very close to current stock). Stop will trigger earlier than greeks-based pricing would. First cycle with fix correctly identified 8 profit takes + 1 stop loss — all IB orders failed due to simultaneous IB disconnect; queued for retry. |
| Jun 16 | App +$292.52 vs IB +$519.66 ($227 gap). NVDA SELL (7a018509) and SPY SELL (9fc3af44) still Active despite TP orders having filled at 4:14 PM and 3:30 PM. NVDA BUY (8dd21dcd) showed wrong pnl=+66.55. SPY BUY (923957b8) showed wrong pnl=-47.69. | EOD sweep (3:55 PM + 4:05 PM) cancelled bracket TPs (53109, 53973) and placed market orders (54122, 54123, 54217, 54218) — all rejected code=200. But TP orders had ALREADY filled (SPY TP at 3:30 PM, NVDA TP at 4:14 PM). Fill ExecDetails never received by auto-trader (race between cancel attempt and fill). `updateIbFillCommission` arrived but found no `ib_fills` row to UPDATE → trigger never fired → paper_trades stayed Active. NVDA BUY and SPY BUY P&L wrong because IB FIFO netted them against prior positions (BOT @ $209.59 closed prior NVDA SHORT; SPY LONG @ $754.96 closed at $754.95 not $752.45). | **DB patches**: (1) 7a018509 NVDA SELL → status=TARGET_HIT, close_price=207.70, ib_pnl=93.53. (2) 9fc3af44 SPY SELL → status=TARGET_HIT, close_price=750.70, ib_pnl=30.95. (3) 8dd21dcd NVDA BUY → ib_pnl=104.89 (IB FIFO attribution). (4) 923957b8 SPY BUY → ib_pnl=-2.49. **Code fix**: `auto-trader/src/ib-connection.ts` `connected` handler now calls `reqExecutions(7999, {})` 10s after every connect — replays today's fills (ExecDetails + commissionReport) so missed TP/SL fills self-heal into ib_fills automatically. | (no PR — hotfix) | Remaining gap after patches = ~$19 (commission differences across 13 trades, acceptable). `pnl` field on patched records updated by existing DB trigger (formula-based) but display uses `ib_pnl` which is correct. |
| Jun 16 | Recurring Today's Activity vs IB P&L gap — trigger overwrote `pnl` with IB FIFO `realized_pnl` on every `commissionReport`, so any manual DB patch was erased and FIFO netting across positions produced numbers that didn't match per-trade formula. Manual DB patches were also at risk of being silently overwritten. | **Root cause: trigger wrote IB's FIFO `realized_pnl` into `paper_trades.pnl`**, which is supposed to be formula-based `(close_price - fill_price) * qty`. When IB FIFO-nets across multiple lots (e.g. PLTR SHORT partially closes a prior LONG from the same session), `realized_pnl` doesn't correspond to any single paper_trade's formula. The `ib_close_order_id = NULL` workaround from Jun 16 DB patches only protected specific records; new closes still wrote to `pnl`. | `supabase/migrations/20260616000001_ib_pnl_column.sql`: added `ib_pnl NUMERIC` to `paper_trades` and `live_trades`. `supabase/migrations/20260616000002_trigger_write_ib_pnl.sql`: trigger now writes `ib_pnl` (IB FIFO) while leaving `pnl` (formula) untouched. `reconcile-executions.ts`: all IB-sourced P&L updates also write `ib_pnl`. `scheduler.ts`: EOD cron passes `ibRealizedPnl` to reconciler; reconciler adds server-side gap check (writes warning event if `SUM(COALESCE(ib_pnl,pnl))` vs IB gap > $5). `TodayActivityTab.tsx`: row P&L, header total, sort, and gainers/losers filter all use `ib_pnl ?? pnl`. `shared/trade-types.ts`: added `ib_pnl` field. | #455 | `pnl` column stays formula-based and will no longer match IB exactly for FIFO-netted trades — this is intentional. Display uses `ib_pnl` when populated. Manual DB patches to `pnl` are now safe (trigger no longer touches `pnl`). The Jun 16 `ib_close_order_id = NULL` patches on e851e101/817f9dc3/8dd21dcd/5c45d083 can be restored if needed — they are no longer necessary as a protection mechanism. |

| Jun 24 | No day trades or swing trades firing all day despite valid signals (MSFT, META, IWM all detected but blocked) | `assessDrawdownMultiplier` fell back to ALL IB positions when `_trackedIbTickers` was empty (no active DAY/SWING positions). Underwater LONG_TERM holds (AOS -25%, ALLY -12%) pegged intraday drawdown at -28.6% → critical gate (multiplier=0) blocked every new entry. PR #383 had already added SHORT_TERM_MODES filtering for the non-empty case but left the empty-set fallback untouched. | `auto-trader/src/scheduler.ts` `assessDrawdownMultiplier`: added `if (_trackedIbTickers.size === 0) return normal` before the filter. | #471 | **Do NOT change the fallback back to `positions` (all IB positions).** When no short-term positions exist, there is nothing to protect against — return normal. See also PR #383 (first half of this fix). |
| Jun 24 | IWM, GOOGL, PLTR, HOOD OPTIONS_SCALP positions showing as CLOSED in Today's Activity but still open in IB | `_syncPositionsForAccount` excluded OPTIONS_PUT/CALL/CREDIT_SPREAD but NOT OPTIONS_SCALP. The sync searched for these OPT contracts as STK positions, never found them, set `missing_since`, then fired the 30-min fallback close using current stock price as `close_price` — completely wrong for options P&L. All 4 records closed in DB with bad P&L while IB still held the option contracts. | `auto-trader/src/scheduler.ts` line 5965: added `OPTIONS_SCALP` to the exclusion list. DB patch: reopened all 4 records (status=FILLED, cleared closed_at/pnl/close_price/ib_close_order_id/missing_since). | (pending PR) | OPTIONS_SCALP positions are now fully managed by the scalp manager. The `_syncPositionsForAccount` path was never the right manager for them. |

| Jun 24 | OPTIONS_SCALP buying 2-day (next-Friday) options instead of 0DTE on a Wednesday | PR #463 changed `getTodayExpiry()` to always return `getNearestFridayExpiry()`. Mon–Thu scalps bought 1–4 DTE options (PLTR, HOOD, GOOGL bought Jun 26 chains on Jun 24). Multi-day options carry overnight risk, have expensive premium, need a larger move to double — violates Kay Capitals 0DTE strategy entirely. | `auto-trader/src/lib/options-scalp.ts`: restored `getTodayExpiry()` to return today's actual date. Tickers without a same-day chain get IB code-200 and are skipped. | #472 | **Do NOT revert to `getNearestFridayExpiry()`** — that's the regression. ETFs (SPY, QQQ, IWM) have daily chains every day. Individual stocks on Mon–Thu will be skipped via code-200 — that is correct behavior. |

| Jun 26 | EOD gap check fires false alarm: our total includes -$2,194 expired_worthless P&L but IB reqPnL shows $0 for passive option expiries | When bought options expire worthless, IB records "Expired 2 @ $0" — no closing transaction, no entry in `reqPnL` daily realized P&L. The cost left the account on the buy date. Our DB books pnl=-$430/-$810 etc. on expiry day; IB's daily counter never sees them. Same pattern for `auto_exercised`. Gap check was comparing apples to oranges. | `auto-trader/src/lib/reconcile-executions.ts`: added `.neq('close_reason','expired_worthless')` and `.neq('close_reason','auto_exercised')` to gap check query (same pattern as PR #474 for `ib_fill_auto_created`). | (pending PR) | Our DB still stores the correct lifetime P&L for expired options — this only affects the daily gap check vs IB's reqPnL counter. |
| Jun 29 | QQQ DAY_TRADE stop bracket (78500) triggered at 10:20 AM, IB sold at $710.87 (-$56.78), but auto-trader never received the `execDetails` fill event — ghost-managed position all day, EOD close failed (code-200), DB stuck as FILLED/open | `execDetails` callback for bracket SL fills can be dropped if there's a brief IB API hiccup mid-day while the auto-trader stays connected. The `reqExecutions` replay on reconnect doesn't help because there was no reconnect. `reconcileMissedBracketFills()` already handles this correctly but only ran at startup. | `auto-trader/src/scheduler.ts`: added hourly `setInterval` (9:30–4:30 PM ET) calling `reconcileMissedBracketFills()` to catch any missed bracket fills within ~60 min. DB record manually patched: STOPPED, close_price=710.87, pnl=-54.60, ib_pnl=-56.78. | (pending PR) | Any future missed TP/SL fill will be auto-recovered within 60 min instead of being orphaned until EOD. |
| Jun 29 | ORB scalp scanner burning premium on individual stocks Mon–Thu — APP -$990, MSFT -$785, GOOGL -$810, PLTR/HOOD -$954 all expired worthless. ETF-only filter was already applied to VWAP scanner (PR #473) but ORB scanner still loaded full HIGH_VOL watchlist every day. Individual stocks (non-ETFs) only have Friday weekly chains; buying 0DTE on them Mon–Thu means no daily chain exists and any fills carry 1–4 DTE risk | `auto-trader/src/lib/options-scalp.ts`: in `runOrbScalpScan()`, filter HIGH_VOL watchlist to `VWAP_ETF_ONLY_UNIVERSE` on Mon–Thu. On Fridays, run full watchlist (weekly 0DTE available for all). Same pattern already applied to VWAP scanner. | (pending PR) | Mon–Thu ORB scans now only try SPY/QQQ/IWM/SMH/SOXL/TQQQ — better liquidity, guaranteed daily chains. |
| Jun 26 | 4 expiry-day scalps (IWM, GOOGL, PLTR, HOOD) not closing all day — management cycle silent | `getOptionGreeksForContract` returns null for near-worthless OTM 0DTE options (IB stops sending option computation events for dead contracts). `manageScalpPositions` hit `if (!greeks) continue` with zero log output — silently skipped every 15-min cycle. Positions were placed Jun 24 with wrong Friday expiry (pre-fix); PLTR $117C, HOOD $100C moved OTM. EOD close would have handled at 3:45 PM, but management should also act. | `auto-trader/src/lib/options-scalp.ts`: added expiry-day handling in `manageScalpPositions` — when Greeks are null AND `option_expiry <= today`, close immediately as `auto_exercised` or `expired_worthless` using stock price. Added logging for null-greeks and null-quote skips. | (pending PR) | The 4 positions will now close at the next 15-min management cycle (12:00 PM) rather than waiting for 3:45 PM EOD sweep. |
| Jun 25 | SYSTEM row showing false EOD gap $337.41 — "our total $1157.39 vs IB $819.98" | Gap check query included `ib_fill_auto_created` ghost records alongside the real `profit_take` records for EXP and PGR. Both were CLOSED today with P&L — ghost ($48.19 + $281.63 = $329.82) plus real ($49.20 + $282.71) = $1157.39 instead of correct $827.57. Gap vs IB is actually ~$7.59 (commissions only). | `auto-trader/src/lib/reconcile-executions.ts`: added `.neq('close_reason', 'ib_fill_auto_created')` to gap check query. | (pending PR) | Ghost records are excluded from gap sum. Real `profit_take` records still counted. SYSTEM warning will not fire for commission-level differences. |

| Jul 1 | ADI OPTIONS_PUT (+$504.44) missing from Today's Activity — IB shows close at 9:46 AM, our UI shows $215.74 vs IB $717.98 | `ibBuyToCloseOption` placed 4 consecutive close orders (87592→87769→87945→88123) each timing out, each stamping a new `ib_close_order_id`. Order 88123 finally filled at $2.80. `ib_fills` row was inserted with `realized_pnl=504.445`. BUT trigger Section 4 only writes `ib_pnl` to records with `status IN ('CLOSED', 'TARGET_HIT', 'STOPPED')` — ADI was still `FILLED`. The trigger silently did nothing. Same root cause as Jun 12 GOOGL OPTIONS_PUT (patched manually each time). | DB patch: `50337128` → CLOSED, close_price=2.80, pnl=505.00, ib_pnl=504.45, close_reason=21dte_forced_close. **Permanent fix:** new migration `20260701000001_trigger_close_filled_on_btc_fill.sql` extends trigger Section 4 with a second UPDATE that sets `status=CLOSED` when `ib_close_order_id` matches and `status='FILLED'`. Auto-heals any future timed-out BTC/STO options closes. | none |

| Jul 8 | QQQ (-$230) and IWM (-$48) OPTIONS_SCALP showing wrong P&L in Today's Activity; IB not showing these as losses; 0DTE buys showed as GTC in IB | `closeBasketballPositionsEod` in `spy-basketball.ts` had 5 bugs: (1) query lacked `.eq('ticker', TICKER)` — grabbed ALL OPTIONS_SCALP trades with today's expiry, including IWM/QQQ VWAP scalp positions; (2) close order hardcoded `symbol: TICKER` (SPY) — dispatched `SELL 1x SPY $293P` and `SELL 1x SPY $706P` instead of the actual IWM/QQQ contracts → orders rejected; (3) hardcoded `contracts: 1` instead of `pos.option_contracts` (actual qty = 2) → P&L calculated for 1 contract only; (4) `fill_price: currentPremium` overwrote entry fill_price with 0 (should be `close_price`); (5) "mark CLOSED anyway" anti-pattern — DB updated regardless of IB fill confirmation. Secondary: `placeOptionsOrder` hardcoded `tif: GTC` for all options including 0DTE. IB's `reqPnL` does not count expired worthless options (same as Jun 26). | **Code fix**: `spy-basketball.ts` `closeBasketballPositionsEod` — added `.eq('ticker', TICKER)` to query, use `pos.option_contracts` for qty, changed `fill_price` → `close_price`, fixed pnl formula to multiply by contracts, removed "mark CLOSED anyway". `ib-connection.ts` `placeOptionsOrder` — default TIF is now DAY for same-day expiry, GTC otherwise (optional `tif` override). **DB patch**: IWM `aee02ad5` → fill_price=0.48, close_price=0, pnl=-96, close_reason=expired_worthless. QQQ `2c709991` → fill_price=2.30, close_price=0, pnl=-460, close_reason=expired_worthless. | pending | IWM $293P was showing ITM at 5:00 PM — if user did not submit Lapse by 5:30 PM, IB may auto-exercise creating a short 200 IWM position with no tracking record. If `reconcileIBShorts` fires on next startup it will flag it. True correct P&L for IWM depends on exercise outcome. |
| Jul 13 | GOOGL +$108.55 missing from Today's Activity (IB shows it); NET/DIA/AAPL show as Pending though IB never filled them | (1) GOOGL $335P entry order 52359 filled **qty=2** but paper_trade `f769e309` tracked qty=1. Jul 8 GTC BTC #103274 stamped; Jul 9 manage cycle placed #108139 **without cancelling #103274**, overwrote `ib_close_order_id`. Jul 10 #108139 closed tracked contract (+$63.55). Jul 13 orphaned #103274 filled 2nd contract (+$108.55) — no paper_trade match. (2) Orphan fill UI only surfaced `side=SLD`, so BOT-to-close shorts were invisible. (3) Unfilled DAY brackets expired as `CLOSED`/`manual` → UI showed Pending/Closed instead of Never filled. | **DB patch**: created `a44ea7a6` GOOGL OPTIONS_PUT CLOSED ib_close=103274 ib_pnl=108.55; NET/DIA/AAPL → CANCELLED/`never_filled`. **Code**: `options-manager` skip manage when `ib_close_order_id` set + cancel prior GTC before restamp; `getTodaysOrphanedFills` include BOT with realized_pnl; scheduler expire unfilled day/swing as CANCELLED/`never_filled`. | pending | Entry qty undercount (2 vs 1) still unfixed at source — audit option entry fill qty vs paper_trade.quantity separately. |
| Jul 13 | MELI SWING_TRADE (8 sh @ $1764.64) stuck `FILLED` since Jul 2 though IB is flat; AOS LONG_TERM tracked 282 open shares vs IB's 149 (133 phantom). Both silently wrong for days/weeks. | **Same root disease, two shapes.** (1) IB dropped the exit `execDetails` (MELI bracket TP filled ~Jul 6; AOS partial exits). `reconcileMissedBracketFills` only replays **today's** `reqExecutions`, so a fill dropped on a prior day is never recovered. (2) The `_syncPositionsForAccount` missing-position guard for SWING/LONG_TERM **reset `missing_since` to null every cycle** — so a genuinely-exited position looped "missing → reset → missing" forever and never reconciled (MELI). (3) AOS's records are internally inconsistent from prior patches (events net 31, ib_fills net 16, FILLED sum 282, IB 149) — only IB (149) is trustworthy. | **DB patch (verified vs live IB first):** MELI `ef94568b` → TARGET_HIT, close_price=1807.48, pnl=+342.72, pnl_source=estimated, closed_at=Jul 6 (real close date, so it does NOT inflate today's P&L). Grounded on bracket TP $1807.48 (high $1812 Jul 6; SL $1734.48 never reached). AOS reconciled to IB's 149: `41b7fda2` (107 sh, no order/fill/event) → CANCELLED reconciled_phantom pnl=0; `69a4b6e9` (26 sh, absorbed by Jun 5 loss-cut of 63 @ -$549.61 already booked) → CLOSED reconciled_ib_flat pnl=0; `5f81fbed` (149 sh) kept open, cost basis aligned to IB avgCost $59.52. **Code fix:** `scheduler.ts` swing/LT missing guard no longer resets `missing_since`; once a position is continuously missing > `STALE_RECONCILE_MS` (4h) AND IB returns a healthy snapshot (other positions present → truly connected), it reconciles at the GROUNDED bracket price via new `resolveStaleBracketExit()` (fetches daily bars, first bar to touch TP/SL wins the OCA; ambiguous same-day → conservative stop; no live-quote guessing — respects the ABBV -$276 lesson). If exit can't be grounded → surfaces a `reconcile_needed` event, does not close. | pending | An over-count detector was prototyped and **removed** before ship: it summed open FILLED qty vs IB, which false-positived on every partial-sell position (PGR/ALLY/ASML/EXP/GE all reconcile once CLOSED SELL profit-take children are subtracted — parent stays FILLED at original qty by design). Lesson: the parent/child model means "FILLED sum > IB" is normal. The stale-reconcile guard only fires when IB holds **0** of a ticker (no parent/child ambiguity); LONG_TERM compounders have null bracket levels so they surface an event rather than auto-close. |
| Jul 13 | ORCL put assignment showing phantom **-$16,467.97** realized loss (reported Thu/Fri; user checked May 28 in IB, found nothing) | `handleAssignment()` computed `assignmentPnl = -(strike - premium - fill) * 100` = `-(177.5 - 6.42 - 6.4) * 100 = -16467.97`. This treats the option **strike** as a per-share P&L term — nonsense. IB never booked any realized loss: on assignment IB folds the premium into the assigned shares' cost basis (IB `avgCost = 177.5 - 6.4 = 171.11`, `realizedPnl = 0`). The share loss is UNREALIZED (~-$3,957 at $131.54) and lives on the IB position, not as realized P&L. Also: the put entry filled **Jun 9** (order 26901), not May 28 — `opened_at` is May 28 but the only `ib_fill` is Jun 9, and there is NO ORCL `ib_fill` on May 28 OR on assignment day Jul 10. | **DB patch**: `edb9c1e8` → `pnl=0`, `ib_pnl=0`, `pnl_source=ib_assignment` (matches IB realizedPnl=0; premium already in share basis; avoids double-count). **Code**: `options-manager.ts handleAssignment()` now sets `assignmentPnl = 0` with `overridePnlSource: 'ib_assignment'`; added `'ib_assignment'` to `PnlSource` union in `trade-closer.ts`. UI needed no change — it renders `ib_pnl ?? pnl`, so the 0 patch clears it everywhere (Today's Activity, History, Performance). | pending | Only 1 assigned record existed, so blast radius = ORCL only. The 100 ORCL shares remain in IB with unrealized loss — that is real and correct; only the phantom **realized** loss was removed. |

---

## Recurring Patterns — Red Flags

If you see any of these, check the log above BEFORE fixing:

| Pattern | Previously caused by | Watch out for |
|---------|---------------------|---------------|
| Credit spread showing wrong P&L | Estimated Greeks (now fixed in #436) | Don't re-add `recordTradeClose` eagerly |
| Credit spread record status=CLOSED unexpectedly | (a) STK filter regression (#432→#433), (b) unknown close_reason=manual path | Check `ib_close_order_id` vs `ib_order_id` — don't confuse entry vs close order |
| Position showing in app but not IB | `reconcileGhostCloses` prematurely closed it | Run `reqPositions()` BEFORE patching DB |
| Position showing in IB but not app | secType mismatch, or record was incorrectly set to CLOSED | Check ib_fills for the order — don't delete records |
| LONG_TERM/SWING_TRADE partial close with wrong qty | Original record mutated (fixed #434) | Never update `quantity` on original — create a child record |
| Ghost record appearing in Today's Activity | ib_fill with no matching `ib_close_order_id` | Check the existing real trade first; don't delete the ghost |

---

## Unresolved Root Causes

These issues were patched at the DB level but the code path that caused them has NOT been identified. They may recur:

1. **NBIS `6ab19f67` spurious close** (Jun 10): Closed with `close_reason=manual`, `pnl=0` within 8 minutes of opening. Suspected: scheduler SUBMITTED detection, but CREDIT_SPREAD is excluded at line 5867. Actual cause unknown.

2. **ASTS third IB mobile row** (Jun 10): IB mobile showed 3 ASTS rows, ib_fills shows only 2 orders (39546, 39550). Could be IB UI splitting execDetails + commissionReport, or a real 3rd fill. Not confirmed.

| Jun 17 (EOD) | NOW/RKLB/NBIS Bear Put P&Ls wrong in DB (~$946 off IB) | DB was patched CLOSED on Jun 16 with estimated P&Ls, but actual IB positions were never closed. All 4 Bear Puts closed in IB today (Jun 17). Accidental NOW Bull Put opened/closed at 3:38-3:56 PM (-$1,445 orphan loss, no paper_trade record). | DB patch: `pnl`+`ib_pnl`+`close_price` on NOW 125/95, RKLB 135/95, RKLB 110/80, NBIS 230/170 | none | Orphan -$1,445 NOW loss exists only in IB, not in paper_trades | | New feature — not a bug fix. Added `bear-put-scanner.ts` with RSI overbought detection, breakdown confirmation, delta-based strike selection, and position manager (75% profit, 50% stop, 3 DTE backstop). Added `BEAR_PUT` skip guard to `manageCreditSpreadPositions` so Bear Put positions aren't double-managed. | `auto-trader/src/lib/bear-put-scanner.ts` (new), `auto-trader/src/scheduler.ts`, `auto-trader/src/lib/credit-spread-scanner.ts` | #467 | Bear Put scan runs Mon-Fri 10:35 AM ET. Position manager runs every 30 min alongside credit spread manager. Watchlist hardcoded (22 high-beta names) until DB migration applied. |

---

## Architecture Hazards (don't repeat these mistakes)

1. **Never add a filter to `syncPositions` for one secType without auditing impact on other modes.** PR #432 (STK filter) is the canonical example — it fixed one thing and broke 7 spreads.

2. **Never call `recordTradeClose` before `ib_close_order_id` is stamped** — fills can arrive in under 100ms.

3. **Never delete a `paper_trade` that has any IB order ID set** — the trigger already fired, deleting orphans the fill permanently.

4. **Never patch the DB without first checking `reqPositions()`** — if IB still holds the position, the record must stay open.

5. **`paper_trades.pnl` has 5 writers** (trigger, `recordTradeClose`, `scheduler.ts`, `credit-spread-scanner.ts`, `reconcile-executions.ts`). Any change to one can be stomped by another. When debugging wrong P&L, check all 5.

6. **`scheduler.ts` is ~8600 lines.** Guards added in one section do not protect other sections. When adding mode-specific logic, search the ENTIRE file for the mode string.

7. **Never patch `status = CLOSED` in the DB without first closing the actual IB position.** Patching the DB ahead of the IB close leaves real open positions in IB with no tracking record. The user sees "orphan" positions, tries to close them manually, and risks accidentally opening new positions from a flat state (Jun 17, 2026: accidental -$1,445 NOW Bull Put opened this way). Always close in IB first, then let the fill trigger update the DB, or verify `reqPositions()` shows zero before writing CLOSED.
